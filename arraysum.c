#include <stdio.h>

int main() {
    int a[n] k;
    scanf("%d,%d",&n,&k);
    int i;
    for(i=1;i<k;i++)
    {for(i=1;i<=n;i++)
    count=i+count;
    }
    printf("sum of natural numbers are:%d",count);
    return 0;
}
